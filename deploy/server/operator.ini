[listener]
bind_host = 0.0.0.0
bind_port = 47001

; Comma-separated CIDR list. Empty = allow any peer (lab default).
; Example: allowed_subnets = 10.0.0.0/24, 192.168.56.0/24
allowed_subnets =

; Max seconds to wait for a reply after sending a command.
command_timeout_seconds = 10
